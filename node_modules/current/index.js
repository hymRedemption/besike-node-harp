module.exports = currentTime;

function currentTime(req, res, next){
	if(req.url === "/current-time"){
		res.end((new Date()).toISOString() + "\n");
	}
	else{
		res.end("Connot Get " + req.url + "\n");
	}
}
